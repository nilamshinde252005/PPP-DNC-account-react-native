import { SafeAreaView } from "react-native";

const PackageScreen = () => {
    const user = {};
    return (
        <SafeAreaView style={{flex: 1}}>
        
        </SafeAreaView>
    );
};

export default PackageScreen;