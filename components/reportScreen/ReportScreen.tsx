import { SafeAreaView } from "react-native";

const ReportScreen = () => {
    const user = {};
    return (
        <SafeAreaView style={{flex: 1}}>
        
        </SafeAreaView>
    );
};

export default ReportScreen;